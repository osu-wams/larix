/**
 * @file
 * Larix theme behaviors.
 */

(function ($, Drupal) {
  'use strict';

  /**
   * Custom behavior for Larix theme.
   */
  Drupal.behaviors.larix = {
    attach: function (context, settings) {
      // Add custom JavaScript behaviors here.
    }
  };

})(jQuery, Drupal);
